#include <iostream>
int main()
{
    double students = (int)15.6;
    // students = students - 100;
    // students = students * 2;
    // students *= 2
    // students = students / 2;
    // students /= 2;
    std::cout
        << students;
}